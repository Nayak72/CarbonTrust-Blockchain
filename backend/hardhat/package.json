{
  "name": "carbon-credit-hardhat",
  "version": "1.0.0",
  "description": "Smart contract for Carbon Credit Registry on Polygon Amoy",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:amoy": "npx hardhat run scripts/deploy.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
